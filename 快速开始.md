# JavTube 快速开始

## 一、环境要求

- Node.js 18+（推荐 20 LTS）
- npm 10+
- Windows 系统（本项目仅支持 Windows）

## 二、安装依赖

```bash
cd javtube_dev
npm install
```

## 三、启动开发

```bash
npm run dev
```

浏览器会打开 http://localhost:5173，同时 Electron 窗口会自动弹出。

## 四、构建生产版本

```bash
# 仅构建前端
npx vite build

# 打包 Electron 应用（输出到 release/ 目录）
npm run build:win
```

## 五、项目结构

```
javtube_dev/
├── electron/              # Electron 主进程
│   ├── main/             # 主进程代码（IPC、数据库、爬虫）
│   └── preload/          # 预加载脚本（暴露 window.api）
├── src/                  # Vue 渲染进程
│   ├── components/       # 通用组件
│   ├── views/            # 页面视图
│   ├── store/            # Pinia 状态管理
│   ├── router/           # 路由配置
│   ├── utils/            # 工具函数
│   └── styles/           # 全局样式
├── public/               # 静态资源（应用图标）
├── build/                # 打包用图标
├── index.html            # HTML 入口
├── vite.config.js        # Vite + Electron 构建配置
├── package.json          # 项目依赖
├── package-lock.json     # 依赖版本锁定
├── 项目说明.md            # 项目说明文档
├── 开发文档.md            # 接口定义和模块说明
└── 快速开始.md            # 本文件
```

## 六、开发流程

1. 修改源码后，Vite 会自动热更新前端
2. 修改 `electron/` 下的主进程代码需要重启 `npm run dev`
3. 数据存储在 `node_modules/electron/dist/data/` 目录（开发模式）

## 七、常见问题

**Q: npm install 报错？**
A: 尝试删除 `package-lock.json` 后重新 `npm install`，或使用 `npm install --legacy-peer-deps`

**Q: Electron 窗口不弹出？**
A: 检查终端输出是否有 `loadFile SUCCESS`，如果有说明已正常启动

**Q: 修改主进程代码不生效？**
A: 主进程代码修改后需要重启 `npm run dev`
