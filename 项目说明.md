# JavTube 源码说明文档

## 项目简介

JavTube 是一个基于 Electron + Vue 3 的影片管理软件，支持影片导入、元数据刮削、标签筛选、收藏管理等功能。

## 技术栈

- **前端框架**: Vue 3 (Composition API, `<script setup>`)
- **构建工具**: Vite 5
- **UI 库**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router 4 (Hash 模式)
- **桌面框架**: Electron
- **数据库**: better-sqlite3 (SQLite WASM)
- **打包工具**: electron-builder

## 目录结构

```
source_code/
│
├── index.html                          # HTML 入口模板
├── package.json                        # 项目依赖和构建配置
├── vite.config.js                      # Vite 构建配置（含 Electron 启动）
├── 项目说明.md                          # 本文件
│
├── electron/                           # ===== Electron 主进程 =====
│   ├── main/
│   │   ├── index.js                    # 主进程入口：窗口创建、IPC 注册、数据目录
│   │   ├── scraper.js                  # 网页爬虫：从 JavBus/JavDb 抓取影片元数据
│   │   └── db/
│   │       ├── init.js                 # 数据库初始化：建表、WASM 加载
│   │       ├── movies.js               # 影片 CRUD：增删改查、筛选、分页
│   │       ├── actress.js              # 女优管理：CRUD、搜索
│   │       └── settings.js             # 设置：配置读写、标签类别、备份恢复
│   └── preload/
│       └── index.js                    # 预加载脚本：通过 contextBridge 暴露 window.api
│
└── src/                                # ===== Vue 渲染进程 =====
    ├── main.js                         # Vue 应用入口
    ├── App.vue                         # 根组件（含路由视图、全局过渡动画）
    │
    ├── router/
    │   └── index.js                    # 路由配置：首页/片库/喜欢/记录/女优/网址/设置/详情
    │
    ├── store/                          # Pinia 状态管理
    │   ├── movies.js                   # 影片 store：列表、筛选、分页、批量操作
    │   └── settings.js                 # 女优 store + 网址 store
    │
    ├── utils/
    │   └── global.js                  # 全局工具：搜索、封面路径解析、番号提取
    │
    ├── styles/
    │   └── global.css                  # 全局样式：重置、变量、通用类
    │
    ├── components/                     # 通用组件
    │   ├── TopNav.vue                  # 顶部导航栏：Logo、标签页、搜索、导入、设置
    │   ├── MovieCard.vue               # 影片卡片：封面、悬停播放、编辑/删除菜单
    │   ├── MovieGrid.vue              # 影片网格：CSS Grid 布局、分页器
    │   ├── SortBar.vue                 # 排序栏：排序方式、随机、重置
    │   ├── StatusBar.vue              # 状态栏：计数、批量操作、多选开关
    │   ├── TagChip.vue                 # 标签芯片：药丸样式标签
    │   ├── TagFilter.vue              # 标签筛选器：按分类分组、多选筛选
    │   ├── AddMovieDialog.vue          # 添加影片对话框：五种导入方式
    │   ├── EditMovieDialog.vue         # 编辑影片对话框
    │   └── AddMovieDialog/             # 添加影片子表单
    │       ├── ScrapeForm.vue          #   在线刮削：输入番号抓取元数据
    │       ├── ManualForm.vue          #   手动添加：填写影片信息
    │       ├── ScanDirForm.vue         #   扫描目录：批量导入视频文件
    │       ├── SingleForm.vue         #   单个视频：选择文件导入
    │       └── NfoForm.vue            #   NFO 导入：解析 XML 文件
    │
    └── views/                          # 页面视图
        ├── Home.vue                    # 首页（建设中）
        ├── Library.vue                 # 片库：影片列表主页面
        ├── Detail.vue                  # 详情：影片详细信息页
        ├── Favorite.vue                # 喜欢：收藏影片列表
        ├── History.vue                 # 观看记录
        ├── Actress.vue                 # 女优管理
        ├── Website.vue                 # 网址导航
        └── Settings.vue                # 设置：基础/标签类别/辅助/关于

```

## 核心架构

### 1. 进程通信 (IPC)

```
渲染进程 (Vue)                     主进程 (Electron)
    │                                  │
    │  window.api.xxx()                │  ipcMain.handle('xxx')
    │  ────────────────────────────>  │
    │                                  │  操作数据库/文件系统
    │  <────────────────────────────  │  返回结果
    │                                  │
```

- `electron/preload/index.js` 通过 `contextBridge` 暴露 `window.api` 对象
- `electron/main/index.js` 注册所有 IPC 处理器
- 渲染进程通过 `window.api.xxx()` 调用主进程功能

### 2. 数据库表结构

- **movies 表**: 影片信息（番号、标题、女优、标签、封面、路径等）
- **actress 表**: 女优信息（名称、头像、描述等）
- **settings 表**: 键值对存储应用设置

### 3. 数据流

```
用户操作 → Vue 组件 → Pinia Store → window.api (IPC) → 主进程 → SQLite
                ↑                                                        │
                └────────────── 响应数据 ←──────────────────────────────┘
```

## 开发指南

### 安装依赖
```bash
cd app
npm install
```

### 开发模式
```bash
npm run dev
```

### 构建生产版本
```bash
npm run build
```

### 打包应用
```bash
npm run pack    # 打包但不生成安装程序
npm run dist    # 打包并生成安装程序
```

## 关键设计决策

1. **标签筛选使用 AND 逻辑**: 影片必须包含所有选中的标签才会显示
2. **导入重复跳过**: 番号已存在时跳过，不更新也不创建副本
3. **分页显示**: 每页 100 条，而非无限滚动
4. **数据存储**: 打包后数据存储在程序同级的 `data/` 目录
5. **全局配色**: 黑色 #1a1a1a 为主色调
6. **圆角规范**: 卡片 12px、对话框 16px、按钮 999px（药丸形）
7. **标签分隔符**: 统一使用中文逗号（，）

## 注意事项

- 本源码仅包含注释后的源代码文件，不包含 `node_modules` 和构建产物
- 使用前需在原项目目录执行 `npm install` 安装依赖
- `package.json` 添加了 JSONC 风格的 `//` 注释，标准 JSON 解析器可能无法解析
- 数据库使用 better-sqlite3 的 WASM 版本，无需原生编译
