# JavTube 开发文档

## 一、能否直接拿到其他电脑开发？

**不能直接开发**，`source_code` 文件夹只包含注释后的源码，缺少以下内容：

| 缺少的内容 | 说明 |
|---|---|
| `node_modules/` | 第三方依赖包，需要 `npm install` 安装 |
| `package-lock.json` | 依赖版本锁定文件，确保跨机器版本一致 |
| `dist/` | 构建产物，`npm run build` 后自动生成 |
| `electron/node_modules/` | better-sqlite3 等 Electron 端依赖 |

**在新电脑上开始开发的步骤：**

```bash
# 1. 将 source_code 文件夹复制到新电脑
# 2. 从原项目根目录复制 package-lock.json 到 source_code/
# 3. 安装依赖
cd source_code
npm install
# 4. 启动开发
npm run dev
```

> 注意：`package.json` 中的 `//` 注释是 JSONC 风格，标准 `npm` 可能无法解析。建议将注释移除或使用 `.jsonc` 扩展名配合支持 JSONC 的工具。

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────┐
│                    渲染进程 (Vue 3)                     │
│                                                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │  Views   │  │Components │  │  Store   │           │
│  │ (页面)    │→ │ (组件)    │→ │ (Pinia)  │           │
│  └──────────┘  └──────────┘  └────┬─────┘           │
│                                    │                  │
│                    ┌───────────────┘                  │
│                    ▼                                  │
│            window.api.xxx()  ← IPC 调用               │
└────────────────────┬────────────────────────────────┘
                     │ contextBridge
┌────────────────────┴────────────────────────────────┐
│                  主进程 (Electron)                    │
│                                                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │ index.js │  │scraper.js│  │  db/*.js │           │
│  │ (入口)    │→ │ (爬虫)   │→ │ (数据库)  │           │
│  └──────────┘  └──────────┘  └────┬─────┘           │
│                                    │                  │
│                            better-sqlite3 (SQLite)     │
└─────────────────────────────────────────────────────┘
```

---

## 三、模块职责

### 3.1 主进程模块 (electron/)

| 文件 | 职责 |
|---|---|
| `main/index.js` | 主进程入口，创建 BrowserWindow，注册所有 IPC 处理器，管理数据目录 |
| `main/scraper.js` | 影片元数据爬虫，从 JavBus/JavDb 抓取标题、封面、女优、标签等 |
| `main/db/init.js` | 数据库初始化，加载 WASM SQLite，建表，持久化到磁盘 |
| `main/db/movies.js` | 影片 CRUD、分页查询、标签筛选(AND逻辑)、批量操作 |
| `main/db/actress.js` | 女优 CRUD、网站收藏 CRUD |
| `main/db/settings.js` | 应用设置读写、标签类别配置、数据库备份/恢复/清空 |
| `preload/index.js` | 通过 contextBridge 暴露 `window.api` 接口给渲染进程 |

### 3.2 渲染进程模块 (src/)

| 文件 | 职责 |
|---|---|
| `main.js` | Vue 应用入口，注册 Element Plus、Pinia、Router |
| `App.vue` | 根组件，包含 TopNav + RouterView + 路由过渡动画 |
| `router/index.js` | 路由配置，8 个页面路由 + 1 个详情动态路由 |
| `store/movies.js` | 影片状态管理：列表、筛选、分页、批量操作 |
| `store/settings.js` | 女优状态管理 + 网址状态管理 |
| `utils/global.js` | 全局工具：搜索、封面路径解析、番号提取 |
| `styles/global.css` | 全局样式重置、CSS 变量、通用类 |

### 3.3 组件模块 (src/components/)

| 组件 | 职责 | Props | Emits |
|---|---|---|---|
| `TopNav.vue` | 顶部导航栏 | 无 | 无（内部 router.push） |
| `MovieCard.vue` | 影片卡片 | `movie: Object` | `play, edit, delete, toggleFav, select` |
| `MovieGrid.vue` | 影片网格容器 | `movies: Array, loading: Boolean` | `page-change, card-action` |
| `SortBar.vue` | 排序栏 | 无 | `sort-change` |
| `StatusBar.vue` | 状态栏 | `total, selectedCount, selectMode` | `toggle-select, batch-delete, batch-scrape, batch-fav` |
| `TagChip.vue` | 标签芯片 | `text, active` | `click` |
| `TagFilter.vue` | 标签筛选器 | `categories, selected, collapsed` | `toggle, toggle-all, toggle-collapse` |
| `AddMovieDialog.vue` | 添加影片对话框 | `modelValue: Boolean (v-model)` | `created` |
| `EditMovieDialog.vue` | 编辑影片对话框 | `modelValue, movie` | `saved` |

### 3.4 页面视图 (src/views/)

| 页面 | 路由 | 职责 |
|---|---|---|
| `Home.vue` | `/` | 首页（建设中，暂为空） |
| `Library.vue` | `/library` | 片库主页面：影片列表、筛选、排序、分页、批量操作 |
| `Detail.vue` | `/detail/:id` | 影片详情：信息展示、在线刮削、播放、编辑 |
| `Favorite.vue` | `/favorite` | 收藏列表（复用 MovieGrid） |
| `History.vue` | `/history` | 观看记录列表 |
| `Actress.vue` | `/actress` | 女优管理：列表、详情抽屉、增删改 |
| `Website.vue` | `/website` | 网址导航：增删改、分类 |
| `Settings.vue` | `/settings` | 设置：基础配置、标签类别、辅助工具、关于 |

---

## 四、接口定义

### 4.1 IPC 接口 (window.api)

渲染进程通过 `window.api` 调用主进程功能。所有调用返回 Promise，结果格式统一为 `{ ok: Boolean, data?: any, error?: string }`。

#### 影片接口 (Movies)

| 方法 | 参数 | 返回值 | 说明 |
|---|---|---|---|
| `getMovies(params)` | `{filter, sort, page, pageSize, onlyFavorite}` | `{ok, data: Array, total: Number}` | 分页查询影片，filter.tagSelected 为 9 类标签数组(AND逻辑) |
| `getMovie(id)` | `Number` | `{ok, data: Object}` | 获取单个影片详情 |
| `createMovie(data)` | `Object` | `{ok, id: Number}` | 创建影片记录 |
| `updateMovie(id, data)` | `id: Number, data: Object` | `{ok}` | 更新影片信息 |
| `deleteMovie(id)` | `Number` | `{ok}` | 删除单个影片 |
| `deleteMovies(ids)` | `Array<Number>` | `{ok}` | 批量删除影片 |
| `getAllTags()` | 无 | `{ok, tags: Array<String>}` | 获取数据库中所有实际存在的标签 |
| `recordPlay(id)` | `Number` | `{ok}` | 记录播放（更新观看时间和次数） |
| `batchSetFavorite(ids, isFav)` | `ids: Array, isFav: Boolean` | `{ok}` | 批量收藏/取消收藏 |
| `batchAddTags(ids, tags)` | `ids: Array, tags: String` | `{ok}` | 批量添加标签 |
| `search(scope, q)` | `scope: String, q: String` | `{ok, data: Array}` | 全局搜索(scope: 'movie'/'actress'/'website') |

#### 女优接口 (Actress)

| 方法 | 参数 | 返回值 | 说明 |
|---|---|---|---|
| `getActressList()` | 无 | `{ok, data: Array}` | 获取全部女优列表 |
| `getActress(id)` | `Number` | `{ok, data: Object}` | 获取单个女优详情 |
| `createActress(data)` | `Object` | `{ok, id}` | 创建女优 |
| `updateActress(id, data)` | `id, data: Object` | `{ok}` | 更新女优 |
| `deleteActress(id)` | `Number` | `{ok}` | 删除女优 |

#### 网址接口 (Websites)

| 方法 | 参数 | 返回值 | 说明 |
|---|---|---|---|
| `getWebsites()` | 无 | `{ok, data: Array}` | 获取全部网址 |
| `createWebsite(d)` | `Object` | `{ok, id}` | 创建网址 |
| `updateWebsite(id, d)` | `id, data: Object` | `{ok}` | 更新网址 |
| `deleteWebsite(id)` | `Number` | `{ok}` | 删除网址 |

#### 设置接口 (Settings)

| 方法 | 参数 | 返回值 | 说明 |
|---|---|---|---|
| `getSettings()` | 无 | `{ok, data: Object}` | 获取全部设置键值对 |
| `updateSetting(k, v)` | `k: String, v: any` | `{ok}` | 更新单个设置项 |
| `getTagCategories()` | 无 | `{ok, data: Array(9)}` | 获取 9 个标签类别配置 |
| `saveTagCategories(cats)` | `Array(9)` | `{ok}` | 保存标签类别配置 |
| `backupDb(targetPath)` | `String` | `{ok}` | 备份数据库到指定路径 |
| `restoreDb(sourcePath)` | `String` | `{ok}` | 从备份恢复数据库 |
| `clearDb()` | 无 | `{ok}` | 清空数据库 |

#### 工具接口 (Utils)

| 方法 | 参数 | 返回值 | 说明 |
|---|---|---|---|
| `playVideo(p)` | `String` | `{ok}` | 用系统播放器打开视频 |
| `scanDir(dir)` | `String` | `{ok, data: Array}` | 递归扫描目录，返回视频文件列表 |
| `readFileText(p)` | `String` | `{ok, data: String}` | 读取文本文件 |
| `readFileBase64(p)` | `String` | `{ok, data: String}` | 读取文件为 Base64 |

#### 对话框接口 (Dialogs)

| 方法 | 参数 | 返回值 | 说明 |
|---|---|---|---|
| `openDirDialog()` | 无 | `{ok, path: String}` | 选择目录 |
| `openVideoDialog()` | 无 | `{ok, path: String}` | 选择视频文件 |
| `openImageDialog()` | 无 | `{ok, path: String}` | 选择图片文件 |
| `openNfoDialog()` | 无 | `{ok, path: String}` | 选择 NFO 文件 |
| `saveDbDialog()` | 无 | `{ok, path: String}` | 选择保存位置 |
| `openDbDialog()` | 无 | `{ok, path: String}` | 选择数据库文件 |

#### 爬虫接口 (Scraper)

| 方法 | 参数 | 返回值 | 说明 |
|---|---|---|---|
| `scrapeMovie(ph, source)` | `ph: String(番号), source: String` | `{ok, data: Object}` | 在线刮取影片元数据 |

#### 其他接口 (Misc)

| 方法 | 参数 | 返回值 | 说明 |
|---|---|---|---|
| `getDataDir()` | 无 | `{ok, path: String}` | 获取数据目录路径 |

---

### 4.2 Pinia Store 接口

#### useMoviesStore (`store/movies.js`)

**State（状态）:**

| 字段 | 类型 | 说明 |
|---|---|---|
| `inited` | Boolean | 是否已初始化 |
| `settings` | Object | 应用设置 |
| `categories` | Array(9) | 9 个标签类别配置 `[{cat, tags: []}]` |
| `allDbTags` | Array<String> | 数据库中实际存在的标签 |
| `movies` | Array<Object> | 当前页影片列表 |
| `total` | Number | 影片总数 |
| `page` | Number | 当前页码 |
| `pageSize` | Number | 每页条数（默认 100） |
| `colsPerRow` | Number | 每行显示列数（默认 5） |
| `loading` | Boolean | 加载状态 |
| `sort` | Object | 排序 `{by, order, random}` |
| `tagSelected` | Array(9) | 每个类别选中的标签 |
| `collapsed` | Array(9) | 每个类别是否折叠 |
| `selectMode` | Boolean | 是否多选模式 |
| `selectedIds` | Array<Number> | 选中的影片 ID |

**Getters（计算属性）:**

| 名称 | 返回 | 说明 |
|---|---|---|
| `pageCount` | Number | 总页数 = ceil(total / pageSize) |
| `visibleCategories` | Array | 过滤后的类别（只含数据库中存在的标签） |

**Actions（方法）:**

| 方法 | 参数 | 说明 |
|---|---|---|
| `initIfNeeded()` | 无 | 首次调用时加载设置和标签类别 |
| `loadAllDbTags()` | 无 | 加载数据库中所有标签 |
| `loadMovies({onlyFavorite, extraFilter, append})` | Object | 加载影片列表，append=true 追加 |
| `toggleTag(catIdx, tag)` | `Number, String` | 切换单个标签选中状态 |
| `toggleCatAll(catIdx)` | `Number` | 切换类别全选/全不选 |
| `resetAll()` | 无 | 重置所有筛选和排序 |
| `deleteMovie(id)` | `Number` | 删除影片并刷新列表 |
| `batchDelete()` | 无 | 批量删除选中影片 |
| `toggleFav(id)` | `Number` | 切换影片收藏状态 |

#### useActressStore (`store/settings.js`)

| 方法 | 说明 |
|---|---|
| `load()` | 加载全部女优 |
| `create(d)` | 创建女优并刷新 |
| `remove(id)` | 删除女优并刷新 |

#### useWebStore (`store/settings.js`)

| 方法 | 说明 |
|---|---|
| `load()` | 加载全部网址 |
| `create(d)` | 创建网址并刷新 |
| `remove(id)` | 删除网址并刷新 |

---

### 4.3 全局工具函数 (`utils/global.js`)

#### `onSearch(scope, q)`
- **功能**: 全局搜索
- **参数**: `scope` - 搜索范围('movie'/'actress'/'website')，`q` - 搜索关键词
- **返回**: `{type: 'nav'|'data'|'empty'|'err', data?: any}`
  - `type='nav'`: 唯一匹配，data 为跳转路径
  - `type='data'`: 多条结果，data 为结果数组
  - `type='empty'`: 空关键词
  - `type='err'`: 搜索出错

#### `resolveCover(cover)`
- **功能**: 将封面路径解析为可显示的 file:// URL
- **参数**: `cover` - 原始路径(HTTP URL / 绝对路径 / 相对路径)
- **返回**: `String` - 可直接用于 `<img src>` 的 URL

#### `extractCode(name)`
- **功能**: 从文件名提取番号（正则匹配 `字母-数字` 格式）
- **参数**: `name` - 文件名
- **返回**: `String` - 番号如 `ABC-123`，无匹配返回空字符串

#### `dataDirRef`
- **类型**: `ref('')`
- **说明**: 全局响应式数据目录路径，App.vue 中初始化

#### `DELIM`
- **值**: `'，'`（中文逗号）
- **说明**: 标签分隔符常量

---

### 4.4 路由定义 (`router/index.js`)

| 路径 | 组件 | 说明 |
|---|---|---|
| `/` | Home.vue | 首页（建设中） |
| `/library` | Library.vue | 片库 |
| `/favorite` | Favorite.vue | 喜欢 |
| `/history` | History.vue | 观看记录 |
| `/actress` | Actress.vue | 女优 |
| `/website` | Website.vue | 网址 |
| `/settings` | Settings.vue | 设置 |
| `/detail/:id` | Detail.vue | 影片详情（id 作为 props 传入） |

---

### 4.5 数据库表结构

#### movies 表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PRIMARY KEY | 自增主键 |
| fh | TEXT | 番号 |
| title | TEXT | 标题 |
| actress | TEXT | 女优名 |
| tag | TEXT | 标签（中文逗号分隔） |
| cover | TEXT | 封面路径 |
| path | TEXT | 视频文件路径 |
| date | TEXT | 发行日期 |
| studio | TEXT | 制片商 |
| series | TEXT | 系列 |
| director | TEXT | 导演 |
| rating | REAL | 评分 |
| runtime | INTEGER | 时长(分钟) |
| cl | TEXT | 收藏状态('y'=收藏, 'n'=未收藏) |
| play_count | INTEGER | 播放次数 |
| last_played | TEXT | 最后播放时间(ISO) |
| created_at | TEXT | 创建时间 |
| notes | TEXT | 简介 |
| yid | TEXT | 女优ID(关联 actress 表) |

#### actress 表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PRIMARY KEY | 自增主键 |
| name | TEXT | 女优名 |
| avatar | TEXT | 头像路径 |
| birthday | TEXT | 生日 |
| height | INTEGER | 身高 |
| bust | TEXT | 罩杯 |
| waist | INTEGER | 腰围 |
| hip | INTEGER | 臀围 |
| debut | TEXT | 出道日期 |
| desc | TEXT | 描述 |

#### settings 表

| 字段 | 类型 | 说明 |
|---|---|---|
| key | TEXT PRIMARY KEY | 设置键名 |
| value | TEXT | 设置值(JSON 字符串) |

---

### 4.6 爬虫接口 (`scraper.js`)

#### `scrapeMovie(ph, source)`
- **功能**: 根据番号从在线网站刮取影片元数据
- **参数**:
  - `ph`: 番号，如 `ABC-123`
  - `source`: 数据源，可选值 `'javbus'` / `'javdb'`
- **返回**: 影片对象，包含以下字段:

```javascript
{
  fh: String,        // 番号
  title: String,     // 标题
  actress: String,   // 女优名
  tag: String,       // 标签（中文逗号分隔）
  cover: String,     // 封面图 URL
  date: String,      // 发行日期
  studio: String,    // 制片商
  series: String,    // 系列
  director: String,  // 导演
  runtime: Number,   // 时长
  yid: String,       // 女优ID
  // ...其他元数据字段
}
```

#### 内部函数

| 函数 | 说明 |
|---|---|
| `twToCn(str)` | 繁体转简体 |
| `deleteSpace(str)` | 去除多余空格 |
| `inteHandler(str)` | 提取数字 |
| `fetchHtml(url)` | 获取网页 HTML |
| `downloadImage(url)` | 下载图片并返回 Base64 |
| `scrapeJavBus(ph)` | 从 JavBus 爬取 |
| `scrapeJavDb(ph)` | 从 JavDb 爬取 |

---

## 五、开发规范

### 5.1 颜色规范

| 用途 | 色值 |
|---|---|
| 主色调（选中/强调） | `#1a1a1a` |
| 默认文字 | `#333` |
| 次要文字 | `#999` |
| 边框 | `#ebeef5` / `#e0e0e0` |
| 背景 | `#ffffff` / `#f5f5f5` |

### 5.2 圆角规范

| 元素 | 圆角 |
|---|---|
| 卡片 | `12px` |
| 对话框 | `16px` |
| 按钮/输入框 | `999px`（药丸形） |
| 圆形按钮 | `50%` |
| 封面图 | `8px` |
| 应用图标 | `18%` |

### 5.3 标签规范

- 标签分隔符统一使用中文逗号 `，`
- 存储时自动将英文逗号转为中文逗号
- 标签筛选使用 AND 逻辑（必须包含所有选中标签）
- 标签按包含影片数降序排列

### 5.4 影片卡片规范

- 封面区域固定宽高比 `3:2`，使用 `object-fit: cover`
- 标题区域最小高度 `2.8em`（2行）
- 悬停按钮：播放、编辑、删除（无详情按钮）
- 按钮固定 `72px × 30px`，居中对齐
- 右下角三点菜单

### 5.5 IPC 通信规范

- 所有 IPC 调用通过 `window.api` 进行
- 返回值统一格式：`{ ok: Boolean, data?: any, error?: String }`
- 渲染进程中使用 `if (window.api)` 判断 API 可用性
- IPC channel 命名格式：`模块:操作`（如 `movies:get`、`settings:update`）

### 5.6 数据存储

- 开发模式：`app/node_modules/electron/dist/data/`
- 打包模式：exe 同级 `data/` 目录
- 数据库文件：`app.db`
- 封面图片：`covers/` 子目录
- 标签类别：`tag-categories.json`
